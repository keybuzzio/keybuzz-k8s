# Base Resources

This directory contains base Kubernetes resources shared across all environments.

## Namespaces

- `core/`: Core system namespaces
- `monitoring/`: Monitoring and observability namespaces
- `data/`: Data-related namespaces

These namespaces are global and not environment-specific.

